spring:  cloud:    config:      uri:      - http://localhost:8888      label: mainmanagement:  endpoints:    web:      exposure:        include:        - health        - refresh  endpoint:    health:      show-details: always
