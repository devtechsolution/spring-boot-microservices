server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/devtechsolution/spring-boot-microservices-config
          username: devtechsolution
          password: maa@2319
          clone-on-start: true
      label: main
      name: config-server