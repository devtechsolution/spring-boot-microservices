server:
  port: 0
spring:
  application:
    name: inventory-service 
  cloud:
    config:
      uri:
      - http://localhost:8888
      label: main
    vault:
      host: localhost
      port: 8200
      scheme: http
      authentication: TOKEN
      token: s.jzdBl08ZiCevaYK7MCiO1SPj
      application-name: product-service
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: http://localhost:8180/auth/realms/microservice-realm

management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
        
  endpoint:
    health:
      show-details: always
      
eureka:
  client:
    register-with-eureka: true
    fetch-registry: true 
  instance:
    instance-id: ${spring.application.name}:${random.uuid}




